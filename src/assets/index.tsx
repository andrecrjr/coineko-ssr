import { staticHostURL } from '@/utils';

export const assetList: { [string: string]: string } = {
	star: `${staticHostURL}/star.svg`,
	cat: `${staticHostURL}/cat.svg`,
	starPurple: `${staticHostURL}/starPurple.svg`
};
